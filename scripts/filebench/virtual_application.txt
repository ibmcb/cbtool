# Parameters for this Virtual Application (Application Instance - AI) type should
# be set on YOUR private configuration configuration file, including the ones 
# commented.

[AI_TEMPLATES : FILEBENCH]

# Attributes MANDATORY for all Virtual Applications
SUT = filebench
LOAD_BALANCER_SUPPORTED = $False
RESIZE_SUPPORTED = $False
REGENERATE_DATA = $True
LOAD_GENERATOR_ROLE = filebench
LOAD_MANAGER_ROLE = filebench
METRIC_AGGREGATOR_ROLE = filebench
CAPTURE_ROLE=filebench
LOAD_PROFILE = fileserver
LOAD_LEVEL = uniformIXIXI1I1
LOAD_DURATION = uniformIXIXI60I90
REPORTED_METRICS = bandwidth,throughput,latency,completion_time

# VApp-specific MANDATORY attributes
DESCRIPTION = Deploys a single VM running the filebench benchmark.\n 
DESCRIPTION +=- LOAD_PROFILE possible values: "fileserver", "oltp_noism", "varmail"
DESCRIPTION +=, "videoserver", "webproxy" (these are the "personalities" used by
DESCRIPTION += filebench to emulate the disk I/O generated by an application).\n
DESCRIPTION +=- LOAD_LEVEL meaning: number of threads generating I/O.\n 
DESCRIPTION +=- LOAD_DURATION meaning: maximum length of time to run (might end
DESCRIPTION += before the maximum, if the amount of data is small or disk fast).\n
DESCRIPTION +=- COMMENT: One of the "Synthetic" Workloads. The filebench benchmark,
DESCRIPTION += due its internal timing idiosyncrasies, might not be able to fully
DESCRIPTION += stress the disk I/O capacity of a single VM.
FILEBENCH_SETUP1 = cb_setup_storage.sh
START = cb_filebench.sh

# VApp-specific OPTIONAL modifier parameters. Commented attributes imply default values assumed
FILEBENCH_DATA_DIR = /fstore
FILEBENCH_DATA_FSTYP = ext4

# Inter-Virtual Application instances (inter-AI) synchronized execution. Entirely optional
#SYNC_COUNTER_NAME = synchronization_counter
#CONCURRENT_AIS = 2
#SYNC_CHANNEL_NAME = synchronization_channel
#RUN_COUNTER_NAME = experiment_id_counter
